// Fill in from Google Cloud Console -> APIs & Services -> Credentials.
// Until set, the "look up on google" button in Add a Spot stays hidden — safe default.
window.PINS_PLACES_CONFIG = {
  apiKey: 'YOUR_GOOGLE_PLACES_API_KEY'
};
